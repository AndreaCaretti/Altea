applications:
  - name: test-redis-cluster
    disk_quota: 200MB
    instances: 1
    memory: 120MB
    health-check-type: process
    stack: cflinuxfs3
    buildpacks:
      - nodejs_buildpack
    services:
      - mtt-redis
